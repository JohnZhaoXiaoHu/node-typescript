{
  "name": "node-typescript",
  "version": "1.0.0",
  "description": "",
  "main": "dist/cluster.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "pm2": "pm2 start dist/cluster.js --log-date-format 'DD-MM HH:mm:ss.SSS'",
    "test": "autocannon -c 200 -d 10 http://localhost:3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autocannon": "^3.2.1",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "@types/node": "^11.12.0",
    "tslint": "^5.14.0",
    "typescript": "^3.3.4000"
  }
}
